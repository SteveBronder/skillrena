---
name: serena-list_dir
description: List files and directories (optionally recursive); use to understand repo structure quickly.
---

# Serena: list_dir

Use this to explore the project layout without running shell commands.

## Tips
- Prefer non-recursive first; recurse only when necessary.
- Enable “skip ignored” (when available) to avoid listing vendored/build artifacts.

